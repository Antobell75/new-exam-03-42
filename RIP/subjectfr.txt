
file_content = """
Sujet du programme `rip`
--------------------------------------------

Fichiers attendus :
    *.c, *.h

Fonctions autorisées :
    puts, write

Objectif :
    Écris un programme qui prend en argument une chaîne de caractères contenant uniquement des parenthèses.

    Si les parenthèses sont déséquilibrées (par exemple : "(()"), ton programme doit supprimer
    le nombre minimal de parenthèses pour que l’expression soit équilibrée.

    Par "supprimer", on entend : remplacer les parenthèses par des espaces.

    Le programme doit afficher toutes les solutions possibles (il peut y en avoir plusieurs).zzz
    L’ordre d’affichage des solutions n’a pas d’importance.

Exemples d’exécution :
(Par souci de lisibilité, le caractère '_' représente une espace et les espaces sont affichés pour mieux visualiser.)

    $> ./rip '( ( )' | cat -e
    _ ( ) $
    ( _ ) $

    $> ./rip '( ( ( ) ( ) ( ) ) ( ) )' | cat -e
    ( ( ( ) ( ) ( ) ) ( ) ) $

    $> ./rip '( ) ( ) ) ( )' | cat -e
    [... 207 lignes restantes ...]

Remarque :
    Ton programme doit gérer les cas où plusieurs suppressions sont possibles, et afficher toutes les expressions valides
    qui peuvent en résulter après suppression minimale.
"""

# Sauvegarde dans un fichier texte
file_path = "/mnt/data/rip_program_french.txt"
with open(file_path, "w") as f:
    f.write(file_content)

file_path
